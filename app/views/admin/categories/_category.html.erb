<p id="notice"><%= notice %> </p> 
<%= turbo_frame_tag "catogroy" do %>
  <p>
  <strong> Name: </strong>
  <%= @category.name %> 
  </p>
  
  <p> 
  <%= link_to 'Edit', edit_admin_category_path(category)%>
  <%= link_to 'Back', admin_categories_path %>
  </p>
<% end %>
<div id= "category">
<%= render @category.category %>
 </div>